/** Dispatched after successful account profile save (see settings + layout listeners). */
export const RIVAL_PROFILE_UPDATED_EVENT = "rival_profile_updated";

/** Dispatched after brand row updates (e.g. `ads_profile_setup` save on workspace hub). */
export const RIVAL_BRANDS_UPDATED_EVENT = "rival_brands_updated";
