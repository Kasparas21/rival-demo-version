/** JSON shape returned by the copy-structure API + Haiku parser. */
export type CopyStructureResult = {
  hook: string;
  body_framework: string[];
  cta_pattern: string;
  emotional_register: string;
  adapt_for_your_brand: string;
};
