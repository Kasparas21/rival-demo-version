/** Minimum captured emails before insights are available. */
export const EMAIL_INSIGHTS_MIN_COUNT = 5;

/** Auto-retry cap before analysis is marked failed. */
export const MAX_AI_ANALYSIS_ATTEMPTS = 3;

/** Default inbox page size. */
export const EMAIL_INBOX_PAGE_SIZE = 50;

/** Max emails included in insights aggregation. */
export const EMAIL_INSIGHTS_MAX_ROWS = 500;

/** Pending analysis batch size per analyze=1 call. */
export const EMAIL_ANALYZE_BATCH_SIZE = 3;
